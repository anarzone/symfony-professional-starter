name: Claude Code Review (DISABLED)

# This workflow is DISABLED
# We now use local pre-push code review via .husky/pre-push hook
# See CODE_REVIEW_WORKFLOW.md for details

on:
  # Trigger on PR comments mentioning @claude
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]

  # Automatic review on PR events
  pull_request:
    types: [opened, synchronize]

  # Manual trigger from GitHub Actions UI
  workflow_dispatch:

# This workflow is disabled - we use local pre-push reviews instead
# To re-enable, remove the 'if: false' condition below
jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Workflow Disabled
        run: |
          echo "This workflow has been disabled."
          echo "We now use local pre-push code review via .husky/pre-push hook"
          echo "See CODE_REVIEW_WORKFLOW.md for more details."
