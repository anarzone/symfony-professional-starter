# This workflow has been DISABLED
# We now use local pre-push code review via .husky/pre-push hook
# See CODE_REVIEW_WORKFLOW.md for details on the local review system

# To re-enable remote code reviews (not recommended):
# 1. Uncomment the 'on:' section below
# 2. Remove the 'if: false' from the job
# 3. Set ANTHROPIC_API_KEY in repository secrets
# 4. Configure the job steps as needed

# on:
#   workflow_dispatch:  # Only allow manual trigger if re-enabled

jobs:
  disabled:
    if: false  # Always disabled - we use local pre-push reviews
    runs-on: ubuntu-latest
    steps:
      - name: Workflow Disabled
        run: |
          echo "This workflow has been disabled."
          echo ""
          echo "We now use local pre-push code review via .husky/pre-push hook."
          echo "This provides:"
          echo "  - Faster feedback (3-5 seconds vs 60+ seconds)"
          echo "  - No API costs"
          echo "  - Interactive control over whether to fix or proceed"
          echo "  - Works offline"
          echo ""
          echo "See CODE_REVIEW_WORKFLOW.md for more details."
